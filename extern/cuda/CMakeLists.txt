add_library(cuda INTERFACE IMPORTED GLOBAL)

foreach(CONFIG ${CMAKE_CONFIGURATION_TYPES})
	file(COPY "${CMAKE_CURRENT_SOURCE_DIR}/lib/cudart64_102.dll" DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/${CONFIG})
endforeach()
